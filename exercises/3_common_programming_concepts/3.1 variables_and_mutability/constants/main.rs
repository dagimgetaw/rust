fn main() {
    const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;
    println!("there are {} seconds in 3 hours", THREE_HOURS_IN_SECONDS);

    // constant are not immutable as a default they are immutable always
    // to create constant variable use all CAPITAL LETTER and _ for spaces
}